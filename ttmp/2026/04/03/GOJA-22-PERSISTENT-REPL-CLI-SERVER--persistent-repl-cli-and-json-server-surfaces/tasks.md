# Tasks

## TODO

- [ ] Add tasks here

- [ ] Design the new single-binary CLI command surface around the shared replsession kernel.
- [ ] Implement the JSON HTTP transport package on top of replsession with session, evaluation, history, bindings, and docs endpoints.
- [ ] Wire CLI commands for repl, eval, history, bindings, docs, export, and restore against the shared kernel.
- [ ] Retire the remaining webrepl prototype package once the new transports are feature-complete and validated.
